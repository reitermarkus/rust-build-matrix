name: Update version tag

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: stairwaytowonderland/normalize-majorver@v1
