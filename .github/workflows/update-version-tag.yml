name: Update version tag

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: stairwaytowonderland/normalize-majorver@f5c295c5d22b5a3eaced869005625a962fa25165 # v1
